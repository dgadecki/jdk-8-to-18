package pl.dgadecki.var;

import java.util.function.BiFunction;

public class Var_02 {

    // private var name = "Harry";
    // private final var name = "Harry";
    // private static final var name = "Harry";

    public static void main(String[] args) {
        // var name;
        // var name = null;

        var city = "Wrocław";
        // city = Long.valueOf(100L);
        // city = "Bochum";

        // BiFunction<Integer, Integer, Integer> fna = (var a, b) -> a > 10 ? a + b : a - b;
        // BiFunction<Integer, Integer, Integer> fna = (var a, var b) -> a > 10 ? a + b : a - b;
    }
}
